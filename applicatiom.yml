spring:
  datasource:
    url: jdbc:mysql://localhost:${mysql.port}/${mysql.database}?useUnicode=true&characterEncoding=UTF-8
    username: ${mysql.username}
    password: ${mysql.password}
    driver-class-name: com.mysql.cj.jdbc.Driver
mybatis:
  mapper-locations: classpath:mapper/*.xml
server:
  port: 8080
#下面是自定义设置
mysql:
  database: creeperhub
  port: 3306
  username: WJZ
  password: 123456
